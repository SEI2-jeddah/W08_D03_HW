
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title></title>
  </head>
  <body>
    <h1>Fruits index page</h1>
    <ul>
      <% fruits.forEach (fruits => { %>
      <li>
        The  <%=fruits.name; %>
        is <%=fruits.color; %> <% if(fruits.readyToEat === true){ %> It is ready
        to eat <% } else { %> It is not ready to eat <% } %><br>
        <a href="./fruitss/<%=fruits._id; %>">Show</a><br>
        <a href="./fruitss/edit<%=fruits._id; %>">edit</a><br>
        <form action="./fruitss/<%=fruits._id; %>?_method=DELETE" method="POST">
            <input type="submit" value="DELETE"/>
        </form>

      </li>
      <% }) %>
    </ul>
    <nav>
      <a href="./fruitss/new">Create a New Fruit</a>
    </nav>
  </body>
</html> 